import React from 'react';
import UserProfile from "@/components/sectional/UserProfile";

const page = () => {
    return (
        <section>
        {/*    UserProfile*/}
            <UserProfile/>
        </section>
    );
};

export default page;
