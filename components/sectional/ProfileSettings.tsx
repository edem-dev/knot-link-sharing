"use client"
import React from 'react';
import ProfileDetails from "@/components/molecular/ProfileDetails";

const ProfileSettings = () => {
    return (
        <section className={"w-full"}>
        {/*    Edit profile details*/}
            <ProfileDetails/>
        </section>
    );
};

export default ProfileSettings;
